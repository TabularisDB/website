import {Urbanist, Outfit, JetBrains_Mono} from 'next/font/google';

export const urbanist = Urbanist({subsets: ['latin'], variable: '--font-sans', display: 'swap'});
export const outfit = Outfit({subsets: ['latin'], variable: '--font-display', display: 'swap'});
export const jetbrainsMono = JetBrains_Mono({subsets: ['latin'], variable: '--font-mono', display: 'swap'});
